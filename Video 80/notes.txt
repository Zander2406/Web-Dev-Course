JavaScript objects have special property called prototype which is either null or references another object. This lets us use the properties of the referenced object inside the object that references it. Works almost like inheritance but not exactly like that.

The this keyword refers to the object itself. It is like self in python