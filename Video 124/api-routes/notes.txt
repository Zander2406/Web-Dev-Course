In Next.js we can make backend applications as well. And in this we have created our Next.js API where we have exposed the add endpoint of the API.

We can use the routes.js file inside a route folder system to expose the API endpoints and after that we can just configure the methods in there.